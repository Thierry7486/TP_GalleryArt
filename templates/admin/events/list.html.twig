{% extends 'admin/base.admin.html.twig' %}

{% block body %}
	<div class="flex w-full h-full justify-center items-center">
		<table class="table-auto">
			<tr>
			<th >id</th>
			<th>nom</th>
			<th>date</th>			
			<th>action</th>			
			</tr>

			{% for event in events %}
			<tr>
			<td >{{ event.id }}</td>
			<td>{{ event.name }}</td>
			<td>{{ event.date|date("m/d/Y") }}</td>
			<td>
			<a href="{{ path('dashboard_update_event', {id: event.id}) }}" class="text-green-500 cursor-pointer">éditer</a>
			<a href="{{ path('dashboard_delete_event', {id: event.id}) }}" class="text-red-500 cursor-pointer">supprimer</a>
			</td>
			</tr>	
			{% endfor %}
		</table>	
	</div>
{% endblock %}

